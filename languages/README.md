# Translations

Place compiled translation catalogs here using the `asteria-financial-platform` text domain. Runtime PHP strings use WordPress internationalization functions. The current JavaScript workstation still requires a complete string-catalog pass before translated releases are claimed.
